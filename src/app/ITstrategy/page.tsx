import ITStrategy from '@/components/ITStrategy/ITStrategy';
import React from 'react';

const page = () => {
    return (
        <>
            <ITStrategy />
        </>
    );
};

export default page;
