import AboutPage from '@/components/AboutPage/AboutPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <AboutPage />
        </div>
    );
};

export default page;
