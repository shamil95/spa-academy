import AboutPage from '@/components/AboutPage/AboutPage';
import React from 'react';

const page = () => {
    return (
        <>
            <AboutPage />
        </>
    );
};

export default page;
